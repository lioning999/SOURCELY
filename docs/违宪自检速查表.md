# 违宪自检速查表
> 提交前必查。替代读架构宪法全文。20 行以内。生命周期：活跃

---

## 一级（破坏架构 — 直接拒绝）
```
□ 越层调用：routes/ 写 SQL？services/ 手管连接？domain/ 读环境变量？
□ 黑名单技术栈：React/Vue/TypeScript/TailwindCSS/jQuery/MongoDB/Celery/LLM SDK？
□ 绕过安全中间件：未 JWT 认证的 /api/* 请求？前端传入 user_id？
```

## 二级（关键缺失 — 24h 修复）
```
□ 用户身份未校验？前端传入 user_id 而非从 request.state 获取？
□ 新建文件未注册？路由注册 / __init__ / 入口引用？
□ 响应格式不一致？不是 {code, data, message}？
□ 密钥硬编码？Google Client Secret / JWT Secret 写在代码里？
```

## 三级（规范 — 提醒修正）
```
□ 函数超过 50 行？
□ 命名不一致？文件名/函数名不符合项目规范？
□ 注释写 WHAT 而非 WHY？
□ 缺少类型注解？
```
